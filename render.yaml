services:
  - type: web
    name: bookhub
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn book.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: DEBUG
        value: "False"
